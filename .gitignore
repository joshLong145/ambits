/target
.serena
.claude
node_modules/

# Dolt database files (added by bd init)
.dolt/
*.db

# Heap probe / perf measurement artifacts — machine-specific.
.perf-baseline/
dhat-heap.json

# Coverage journals — per-session derived state, not shared config.
# `.ambit/tools.toml` is deliberately still committable.
.ambit/coverage/
