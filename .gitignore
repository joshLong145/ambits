/target
.serena
.claude
node_modules/

# Heap probe / perf measurement artifacts — machine-specific.
.perf-baseline/
dhat-heap.json

# Local tool state — per-machine, not shared.
.ambit/
.ambits/
.beads/

# macOS Finder metadata
.DS_Store
